{
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "yarn workspace @oasisprotocol/ui-library dev",
    "build": "yarn workspace @oasisprotocol/ui-library build",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
