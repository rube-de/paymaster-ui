#!/bin/sh

# Run lint-staged and TypeScript check in frontend directory.
# Using --cwd to run from repo root without changing directory.

# Run lint-staged on staged files (ESLint + Prettier)
npx lint-staged --cwd frontend

# Run full TypeScript check (cannot be file-scoped)
yarn --cwd frontend checkTs
